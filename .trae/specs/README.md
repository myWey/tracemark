# Specs

本目录由 Trae 原生 `/spec` 命令自动生成。

每个变更对应一个子目录：`.trae/specs/<change-id>/spec.md` + `tasks.md` + `checklist.md`。

请勿手动创建；请通过原生 `/spec` 命令生成。`spec-driven` skill 仅提供方法论指引，不直接写文件。

> Status: Directory placeholder. Native `/spec` output goes here.
