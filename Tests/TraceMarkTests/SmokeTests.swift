import XCTest

final class SmokeTests: XCTestCase {
    func testPackageLoads() {
        // Placeholder: ensures swift test has at least one runnable test.
        // Meaningful tests for Capture/Annotation/OCR will be added in the engineering-hardening phase.
        XCTAssertTrue(true)
    }
}
